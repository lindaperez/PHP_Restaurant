{% extends '::layout.html.twig' %}

{% block content %}

    <h1>TbEdoServicio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Vestatus</th>
                <td>{{ entity.vestatus }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('EdoServicio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('EdoServicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
