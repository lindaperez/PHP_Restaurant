{% extends '::layout.html.twig' %}

{% block content %}

{% for flashMessage in app.session.flashbag.get('flash_info') %}
<div class="isa_info">
        {{ flashMessage }}
    </div>
<br><br>
{% endfor %}
 

{% for flashMessage in app.session.flashbag.get('flash_warning') %}
<div class="isa_warning">
        {{ flashMessage }}
    </div>
<br><br>
{% endfor %}
 


{% for flashMessage in app.session.flashbag.get('flash_error') %}
<div class="isa_error">
        {{ flashMessage }}
    </div>
<br><br>
{% endfor %}
     
<div class="container" >

    <section class="register">
        <h1>Listado de Roles </h1>
        <br>
    <table class="records_list">
        <thead> 
            <tr>
                <td width="80"><h1><strong>Nro</strong></h1></td>
                <td width="40"></td>
                <td width="80"><h1><strong>Título</strong></h1></td>
                <td width="40"></td>
                <td width="80"><h1><strong>Acciones</strong></h1></td>
                <td width="40"></td>
                
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td width="80" ><a href="{{ path('ens_roles_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td width="40"></td>
                <td width="80">{{ entity.vtitulo }}</td>
                <td width="40"></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ens_roles_edit', { 'id': entity.id }) }}">Ver/Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<br><br>
        <ul>
        <li>
            <a href="{{ path('ens_roles_new') }}">
               Crear Nuevo Rol
            </a>
        </li>
    </ul>
        </section>
    </div>
    {% endblock %}
